# Proposed root marks — a signing sheet

**Nothing here is canon.** Each row is a proposal with a blank reason field.
A row enters the Record when a human signs it — sign, strike, or rewrite,
reason included. Unsigned rows do not exist and the gap stays honest.

The compiler currently resolves all seventeen families to provisional
placeholder geometry. Binding them to authored marks is what would make a
compiled word draw a real mark — and it decides what `signal` *looks like*,
which is Record content and not a thing inference may settle.

## How the evidence was found

The only honest chain available today: a family owns words; the concept table
owns words; where they overlap, the concept's traditions reach marks. That is
**one bridge wide** — `grammar.v1` and the concept table share only 29 words of
248 and 170 — so the evidence below is thin by construction. A family with no
bridge word gets no candidates, and that silence is data too.

Separators own no words at all, so they carry no evidence and appear here only
so the sheet is complete rather than quietly seventeen-minus-four rows long.

**6 of 17** rows have any candidate at all.

---

## `signal` — SIGNAL

> transmission, message, that which is sent

- **kind** root
- **currently draws** `root-signal` (provisional placeholder)
- **bridge words** `word`→mind, `words`→mind
- **candidates** 0

```
SIGN:   signal -> ______________________________
REASON: 
        (a prediction: what would measurably differ if this mark were a
         different one. An adjective grades as Arbitrary.)
```

## `body` — BODY

> vessel, flesh, the container that carries

- **kind** root
- **currently draws** `root-body` (provisional placeholder)
- **bridge words** `body`→earth, `heart`→love
- **candidates** 33
    - `mark-aegishjalmur` — via earth
    - `mark-akhet` — via earth
    - `mark-ankh` — via earth
    - `mark-borromean` — via earth
    - `mark-eyehorus` — via earth
    - `mark-eyeprov` — via earth
    - `mark-flower` — via earth
    - `mark-fruit` — via earth
    - _…and 25 more, all reached the same way_

```
SIGN:   body -> ______________________________
REASON: 
        (a prediction: what would measurably differ if this mark were a
         different one. An adjective grades as Arbitrary.)
```

## `persistence` — PERSISTENCE

> survival, endurance, that which remains

- **kind** root
- **currently draws** `root-persistence` (provisional placeholder)
- **bridge words** none — this family shares no word with the concept table
- **candidates** none. No evidence exists; any mark chosen here would be chosen by taste alone.

```
SIGN:   persistence -> ______________________________
REASON: 
        (a prediction: what would measurably differ if this mark were a
         different one. An adjective grades as Arbitrary.)
```

## `void` — VOID

> absence, silence, the unwritten

- **kind** root
- **currently draws** `root-void` (provisional placeholder)
- **bridge words** `dark`→descent, `darkness`→descent, `void`→shadow
- **candidates** 7
    - `mark-aegishjalmur` — via descent, shadow
    - `mark-valknut` — via descent, shadow
    - `mark-vegvisir` — via descent, shadow
    - `mark-veldismagn` — via descent, shadow
    - `mark-webofwyrd` — via descent, shadow
    - `mark-ouroboros` — via descent
    - `mark-squaredcircle` — via descent

```
SIGN:   void -> ______________________________
REASON: 
        (a prediction: what would measurably differ if this mark were a
         different one. An adjective grades as Arbitrary.)
```

## `threshold` — THRESHOLD

> boundary, gate, the place of crossing

- **kind** root
- **currently draws** `root-threshold` (provisional placeholder)
- **bridge words** `crossing`→threshold, `door`→threshold, `gate`→threshold, `portal`→threshold, `threshold`→threshold, `veil`→threshold
- **candidates** 31
    - `mark-aegishjalmur` — via threshold
    - `mark-akhet` — via threshold
    - `mark-ankh` — via threshold
    - `mark-borromean` — via threshold
    - `mark-eyehorus` — via threshold
    - `mark-eyeprov` — via threshold
    - `mark-flower` — via threshold
    - `mark-fruit` — via threshold
    - _…and 23 more, all reached the same way_

```
SIGN:   threshold -> ______________________________
REASON: 
        (a prediction: what would measurably differ if this mark were a
         different one. An adjective grades as Arbitrary.)
```

## `witness` — WITNESS

> observation, knowing, the one who receives

- **kind** root
- **currently draws** `root-witness` (provisional placeholder)
- **bridge words** none — this family shares no word with the concept table
- **candidates** none. No evidence exists; any mark chosen here would be chosen by taste alone.

```
SIGN:   witness -> ______________________________
REASON: 
        (a prediction: what would measurably differ if this mark were a
         different one. An adjective grades as Arbitrary.)
```

## `structure` — STRUCTURE

> order, lattice, law, the arrangement of parts

- **kind** root
- **currently draws** `root-structure` (provisional placeholder)
- **bridge words** `law`→order, `order`→order, `pattern`→order, `structure`→order
- **candidates** 31
    - `mark-aegishjalmur` — via order
    - `mark-akhet` — via order
    - `mark-ankh` — via order
    - `mark-borromean` — via order
    - `mark-eyehorus` — via order
    - `mark-eyeprov` — via order
    - `mark-flower` — via order
    - `mark-fruit` — via order
    - _…and 23 more, all reached the same way_

```
SIGN:   structure -> ______________________________
REASON: 
        (a prediction: what would measurably differ if this mark were a
         different one. An adjective grades as Arbitrary.)
```

## `transformation` — TRANSFORMATION

> becoming, burning, irreversible change

- **kind** root
- **currently draws** `root-transformation` (provisional placeholder)
- **bridge words** `burn`→fire, `dissolve`→water, `fire`→fire, `forge`→fire, `rise`→return
- **candidates** 33
    - `mark-ouroboros` — via fire, return, water
    - `mark-squaredcircle` — via fire, return, water
    - `mark-aegishjalmur` — via fire
    - `mark-akhet` — via fire
    - `mark-ankh` — via fire
    - `mark-borromean` — via fire
    - `mark-eyehorus` — via fire
    - `mark-eyeprov` — via fire
    - _…and 25 more, all reached the same way_

```
SIGN:   transformation -> ______________________________
REASON: 
        (a prediction: what would measurably differ if this mark were a
         different one. An adjective grades as Arbitrary.)
```

## `duration` — DURATION

> time, cycle, return, the long interval

- **kind** root
- **currently draws** `root-duration` (provisional placeholder)
- **bridge words** `age`→time, `ages`→time, `cycle`→time, `cycles`→time, `eternal`→time, `return`→return, `time`→time
- **candidates** 33
    - `mark-aegishjalmur` — via time
    - `mark-akhet` — via time
    - `mark-ankh` — via time
    - `mark-borromean` — via time
    - `mark-eyehorus` — via time
    - `mark-eyeprov` — via time
    - `mark-flower` — via time
    - `mark-fruit` — via time
    - _…and 25 more, all reached the same way_

```
SIGN:   duration -> ______________________________
REASON: 
        (a prediction: what would measurably differ if this mark were a
         different one. An adjective grades as Arbitrary.)
```

## `intensify` — INTENSIFY

> amplified, absolute, at full magnitude

- **kind** modifier
- **currently draws** `mod-intensify` (provisional placeholder)
- **bridge words** none — this family shares no word with the concept table
- **candidates** none. No evidence exists; any mark chosen here would be chosen by taste alone.

```
SIGN:   intensify -> ______________________________
REASON: 
        (a prediction: what would measurably differ if this mark were a
         different one. An adjective grades as Arbitrary.)
```

## `negate` — NEGATE

> inverted, denied, the root turned against itself

- **kind** modifier
- **currently draws** `mod-negate` (provisional placeholder)
- **bridge words** none — this family shares no word with the concept table
- **candidates** none. No evidence exists; any mark chosen here would be chosen by taste alone.

```
SIGN:   negate -> ______________________________
REASON: 
        (a prediction: what would measurably differ if this mark were a
         different one. An adjective grades as Arbitrary.)
```

## `collective` — COLLECTIVE

> plural, many, the root held in common

- **kind** modifier
- **currently draws** `mod-collective` (provisional placeholder)
- **bridge words** none — this family shares no word with the concept table
- **candidates** none. No evidence exists; any mark chosen here would be chosen by taste alone.

```
SIGN:   collective -> ______________________________
REASON: 
        (a prediction: what would measurably differ if this mark were a
         different one. An adjective grades as Arbitrary.)
```

## `anterior` — ANTERIOR

> prior, already elapsed, the root in the past

- **kind** modifier
- **currently draws** `mod-anterior` (provisional placeholder)
- **bridge words** none — this family shares no word with the concept table
- **candidates** none. No evidence exists; any mark chosen here would be chosen by taste alone.

```
SIGN:   anterior -> ______________________________
REASON: 
        (a prediction: what would measurably differ if this mark were a
         different one. An adjective grades as Arbitrary.)
```

## `clause-break` — CLAUSE BREAK

> end of a clause

- **kind** separator
- **currently draws** `sep-clause-break` (provisional placeholder)
- **bridge words** none — this family shares no word with the concept table
- **candidates** none. No evidence exists; any mark chosen here would be chosen by taste alone.

```
SIGN:   clause-break -> ______________________________
REASON: 
        (a prediction: what would measurably differ if this mark were a
         different one. An adjective grades as Arbitrary.)
```

## `relation` — RELATION

> binds two roots into one reading

- **kind** separator
- **currently draws** `sep-relation` (provisional placeholder)
- **bridge words** none — this family shares no word with the concept table
- **candidates** none. No evidence exists; any mark chosen here would be chosen by taste alone.

```
SIGN:   relation -> ______________________________
REASON: 
        (a prediction: what would measurably differ if this mark were a
         different one. An adjective grades as Arbitrary.)
```

## `mirror-axis` — MIRROR AXIS

> the axis a mirrored passage reflects across

- **kind** separator
- **currently draws** `sep-mirror-axis` (provisional placeholder)
- **bridge words** none — this family shares no word with the concept table
- **candidates** none. No evidence exists; any mark chosen here would be chosen by taste alone.

```
SIGN:   mirror-axis -> ______________________________
REASON: 
        (a prediction: what would measurably differ if this mark were a
         different one. An adjective grades as Arbitrary.)
```

## `void-gap` — VOID GAP

> an interval of deliberate silence

- **kind** separator
- **currently draws** `sep-void-gap` (provisional placeholder)
- **bridge words** none — this family shares no word with the concept table
- **candidates** none. No evidence exists; any mark chosen here would be chosen by taste alone.

```
SIGN:   void-gap -> ______________________________
REASON: 
        (a prediction: what would measurably differ if this mark were a
         different one. An adjective grades as Arbitrary.)
```

---

_Generated by `scripts/propose-root-marks.ts`. Regenerating is byte-identical._
_Signing happens here, by hand. Nothing reads this file back yet — an ingest_
_step gets built once there are signed rows to ingest._

