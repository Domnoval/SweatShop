/**
 * @studio137/render-raster
 *
 * Deterministic rasterization of the canonical SVG into a production PNG, with
 * mandatory metadata scrubbing. Trusted-process only (spec §15, §23).
 */

export * from "./png.js";
export * from "./rasterize.js";
