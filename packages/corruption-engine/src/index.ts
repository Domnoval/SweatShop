/**
 * @studio137/corruption-engine
 *
 * Grammatical corruption, recorded occlusion, non-semantic decoys, and surface
 * atmosphere. Every operation is planned against node identifiers and layout
 * bounds — never against path data — so the canonical vector master is
 * structurally out of reach (spec §7).
 */

export * from "./types.js";
export * from "./bands.js";
export * from "./geometry.js";
export * from "./planner.js";
